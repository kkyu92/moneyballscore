# v1.8 cohort baseline — canonical method + cohort cleanup (plan #14 C1c)

> **Closure (cycle 1460, 2026-07-06)**: v1.8 유지 확정. n=178 도달 (cycle 1447) + plan #16 2차 fire → DEFAULT vs SHADOW_V20 Brier 차이 0.15% < 1pp → **v2.0 전면 재조정 불필요**. 본 문서 body 안 "n=150 wait" / "cleanup 후 재추정" / ETA 표현은 cycle 1021 시점 측정 기록 (역사 evidence 보존).

cycle 1021 hotfix 후속 — plan #14 C1c Step A 식별 + **Step B UPDATE 완료** (2026-05-28).

## Step A 진단 결과 (UPDATE 전)

- v1.8 production row total: **54** (pre_game cohort)
- credit-fail candidate: **26 row (48%)**
- signal 분포: `debate_agents_failed`=26, `debate_agent_error`=26 (전수 LLM agent fallback)

상세 candidate ID + reason = `docs/research/v1.8-credit-fail-candidates.json` 박제 (`scripts/cohort-cleanup.ts` Step A dry-run 결과).

## Step B UPDATE 결과 (실행 완료)

- 26 row UPDATE `scoring_rule='v1.8-credit-fail'` (failed=0)
- v1.8 production cohort 정합성 회복

### 사후 per-rule accuracy (2026-05-28 측정)

| scoring_rule | total | verified | hits | accuracy |
|---|---|---|---|---|
| v1.5 | 24 | 16 | 12 | 75.00% |
| v1.6 | 46 | 46 | 17 | 36.96% |
| v1.7-revert | 35 | 34 | 19 | 55.88% |
| **v1.8 (clean)** | **28** | **25** | **11** | **44.00%** |
| **v1.8-credit-fail** | **26** | **22** | **13** | **59.09%** |
| v2.1-B-shadow | 54 | 47 | 24 | 51.06% |
| v2.0-shadow | 5 | 0 | 0 | n/a |

### 발견

- **v1.8 (clean) baseline = 44.00% n=25 verified** — cycle 989 박제 (n=27, 48.1%) 보다 약간 낮음. 표본 차이 가능 (cycle 989 시점 측정 method 차이)
- **v1.8-credit-fail accuracy 59.09% > v1.8 (clean) 44.00%** — credit-fail row 의 quant fallback 가 오히려 LLM debate 보다 정확. **표본 작아서 noise 가능성 vs LLM debate value-add 의문 evidence**. 자가 의심: n=22 vs n=25 = 비교 결정 X
- **v2.0-shadow 5 row verified=0** — 어제 박제됐지만 verify 안 됨. backfill script 재실행 또는 다음 daily cron 자연 verify wait

## 측정 method drift 진단

| cycle | n 측정 | method (추정) |
|---|---|---|
| cycle 861 | n=32 | (method drift, 측정 layer 차이) |
| cycle 989 | n=27 | pre_game cohort only / pending row 제외 |
| cycle 994 | n=94 | all cohort 포함 (v1.8 credit-fail 15건 분리 시 79 추정) |

→ method 차이 = (a) cohort 범위 (pre_game vs all) (b) credit-fail row 포함/제외 (c) pending row (is_correct=null) 포함/제외

## Canonical method (post-cleanup 박제 기준)

**v1.8 baseline cohort 정의** (cleanup 후):

```sql
SELECT *
FROM predictions
WHERE scoring_rule = 'v1.8'      -- credit-fail 분리됐으므로 'v1.8-credit-fail' 제외 자동
  AND prediction_type = 'pre_game'
  AND is_correct IS NOT NULL      -- verified 만 (pending 제외)
```

**v1.8 accuracy 측정**:
```sql
SELECT
  COUNT(*) AS n,
  SUM(CASE WHEN is_correct THEN 1 ELSE 0 END) AS hits,
  ROUND(AVG(is_correct::int) * 100, 2) AS accuracy_pct
FROM predictions
WHERE scoring_rule = 'v1.8'
  AND prediction_type = 'pre_game'
  AND is_correct IS NOT NULL;
```

## Step B execution 완료 (2026-05-28 cycle 1021 plan #14 C1c)

선행 조건 모두 완료:

1. ✅ **tuple bump 완료**: `ALL_SCORING_RULES` 에 `'v1.8-credit-fail'` literal 추가 (`packages/shared/src/model-version-labels.ts:18`)
2. ✅ **cohort filter consumer 갱신**: `SCORING_RULE_HEATMAP_ROWS` + `VERSION_NOTES` map 안 `'v1.8-credit-fail'` row 추가 (`apps/moneyball/src/lib/accuracy/buildAccuracyData.ts:288,464`)
3. ✅ **mig 030 composite UNIQUE 검증**: `(game_id, prediction_type, scoring_rule)` — `v1.8` → `v1.8-credit-fail` UPDATE 안 충돌 없음 (다른 scoring_rule)
4. ✅ **test 7 version 갱신**: `buildVersionHistory` test + `buildScoringRuleDayHeatmap` test 갱신 (714/714 PASS)
5. ✅ **rollback path 박제**: 아래 SQL

## Rollback SQL

UPDATE 잘못 박제 시 1 line rollback:

```sql
UPDATE predictions
SET scoring_rule = 'v1.8'
WHERE scoring_rule = 'v1.8-credit-fail';
```

## script 재사용

```bash
# Step A 식별 (dry-run, default)
pnpm tsx scripts/cohort-cleanup.ts

# Step B UPDATE 실행 (사용자 confirm 후, tuple bump 선행)
pnpm tsx scripts/cohort-cleanup.ts --write
```

env: `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (자동 로드: `.env.local`)

## 사용자 가시 영향 (Step B 실행 후 예측)

- v1.8 baseline accuracy 재측정 = 26 credit-fail 분리 후 진짜 v1.8 cohort 정합
- /accuracy 페이지 안 scoring_rule 분포 = v1.8 28 row / v1.8-credit-fail 26 row (이전 v1.8 통합 54 row)
- BrierTrendChart / ScoringRuleDayHeatmap 안 v1.8-credit-fail cohort 별도 line 추가 → 가시화

## 자가 의심

- candidate 26/54 = 48% credit-fail 비율 = 매우 높음. ANTHROPIC API credit 한도 도달 또는 retry exhaustion 시점 cluster 가능성. created_at 분포 분석 권장 (`candidates[].created_at` 시간대별 분포)
- signal grep 보수적 정의 (debate_agents_failed only) — 다른 silent fallback path 누락 가능성 (예: schedule.ts window_too_late row 의 reasoning structure 분석)
- 본 cleanup 후 v1.8 baseline n=28 = 더 작은 표본 → forward cohort n=150 wait 시간 더 길어짐 (current ETA 2026-08-04 → cleanup 후 재추정) ← stale: n=178 crossed (cycle 1447), v1.8 유지 확정 (cycle 1460) — ETA 무의미, wait 종료

## 참조 plan #14 C1c body

`~/.develop-cycle/plans/moneyballscore/14.md` line 121~132.
