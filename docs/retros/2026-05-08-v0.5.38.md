# Retro v0.5.38 — cycle 256 operational-analysis lite

**날짜**: 2026-05-08  
**체인**: operational-analysis (lite)  
**트리거**: 마지막 operational-analysis(cycle 231) 이후 25 사이클 경과 + cycle 255 retro `next_recommended: operational-analysis`

---

## What (이번 사이클에서 한 것)

1. **W19 최종 성과 분석** (2026-05-05~05/07, 14경기)
   - 총 8/14 = 57.1% (시즌 평균 48.6% 대비 +8.5%p)
   - Supabase REST API 직접 쿼리로 predictions + games 데이터 집계
   - 시리즈별/요일별/팩터별 분석

2. **SFR 극단값 편향 패턴 발견 및 박제**
   - SSG vs NC 3연전: sfr=0.72~1.0 극단값이 head_to_head=0.33(NC 우세) 신호 오버라이드 → 0/3 전패
   - `docs/lessons/2026-05-08-sfr-extremes-override-pattern.md` 작성

3. **CHANGELOG.md v0.5.38 업데이트**
   - W19 시리즈별 결과 테이블
   - 패턴 4건 박제 (SFR 편향 / head_to_head 재평가 / 일요일 저적중 / n=76 진행 현황)

4. **TODOS.md 업데이트**
   - 검증 건수 72 → 76 갱신
   - SFR 극단값 대응 후보 항목 신규 추가
   - head_to_head 가중치 방향 재검토 경고 추가

---

## Decisions (결정 사항)

1. **가중치 변경 없음** — n=76 < 150 임계. 패턴이 발견됐어도 소표본(95% CI ±15%p) 경고 기준 변경 보류.

2. **SFR 재검토 방향 반전**: cycle 231 결론("sfr 중립 = 5% 유지")에서 cycle 256 발견("sfr 극단값 케이스 편향")으로 우선순위 상향. n=150 후 별도 케이스 분류 필요.

3. **head_to_head 감소 방향 재검토**: cycle 231 "head_to_head 5%→3% 감소 후보"가 W19 데이터에선 반대 신호 — TODOS에 ⚠️ 표시만, 방향 미결정.

---

## Worked (잘 된 것)

- **trigger 설계 정확도**: cycle 255 skill-evolution이 추가한 "25 사이클 주기 보정 trigger"가 cycle 256에서 정확히 작동 (25 사이클 = cycle 231→256)
- **Supabase REST API 직접 쿼리**: DB 접근 없이 REST로 predictions/games/teams 전부 조회 — 데이터 정확
- **시리즈 단위 분석**: 개별 경기 단위보다 시리즈(3연전) 단위로 보니 편향 패턴이 명확하게 드러남

---

## Didn't Work (아쉬운 점)

- **데이터 신선도 이슈**: cycle 231이 2026-05-07(어제)이라 오늘 분석은 단 1일 차이. 새 인사이트가 제한적. 25 사이클 주기 보정 trigger가 의미 있으려면 cycle 간격이 날짜 간격과 일치해야 함 (현재 ~1 cycle/1일 속도면 25 사이클 = 25일이 이상적이나 이번엔 1일만 차이남).

- **confidence 분포 분석 미완**: confidence 값들이 0.4~0.6 구간에 밀집 → 고확신/저확신 분류 의미 없음. 실제 homeWinProb 기반 재분류 필요.

---

## Next Candidates (다음 후보 3가지)

1. **fix-incident**: 0/20 fire 기록 — 파이프라인 cron 실패, 일요일 경기 예측 누락, live-update silent skip 가능성 점검 필요. Cloudflare Worker 안정성 확인.

2. **review-code (heavy)**: `packages/kbo-data/src/agents/` 주요 파일들(debate.ts / judge-agent.ts / retro.ts) 주석 vs 코드 mismatch 가능성. 특히 judge-agent의 SFR 처리 로직.

3. **explore-idea**: 사용자 AdSense 심사 대비 콘텐츠 깊이 강화 — `TODOS.md #5` "콘텐츠 깊이 보강" 지속. 또는 일요일 경기 confidence clamp 기능 prototype (n<150이어도 soft-launch 가능한 형태).
