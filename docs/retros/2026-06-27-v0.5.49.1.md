# Retro: v0.5.49.1 — cycle 1400 operational-analysis (2026-06-27)

## What

- **주간 리뷰 2026-W26** (cycle 1400): v1.8 이번 주 75.0% (15/20), 누적 59.0% (n=117)
- **judge-agent 토론 22일 비활성 발견**: 2026-06-06부터 confidence=0.3 flat — DB 집계로 탐지
- **P1-P4 패턴 추출 CHANGELOG 기록**: sparse detection / silent LLM fallback / registry sweep / alert cascade
- **docs/solutions/silent-llm-fallback-masking.md** 신규 anti-pattern 문서화

## Decisions

| 결정 | 이유 |
|---|---|
| 가중치 조정 보류 | n=117 < 150 (잔여 33건, ETA ~1.6주) |
| judge-agent 조사 이번 사이클 X | op-analysis lite 범위 — fix-incident 다음 사이클 |
| v2.0/v2.1-B shadow 비교 보류 | 3모델 예측 완전 일치 — debate 복구 후 비교 의미 있음 |
| 75% 성과 v1.8 가중치 공로 인정 | 정량 모델 단독 성과 → ablation evidence 박제 |

## Worked

- **DB 집계로 silent LLM failure 탐지**: `DISTINCT confidence` 쿼리 — 22일 미감지 이슈를 op-analysis에서 자연 발견
- **요일별 분석**: 화60% / 수목금 80% 패턴 — 주초 취약 지속 확인
- **3모델 예측 완전 일치 확인**: v1.8 / v2.0-shadow / v2.1-B-shadow 모두 동일 — debate 없이 정량 모델 수렴

## Didn't Work

- **judge-agent 토론 22일 미감지**: `pipeline_runs.errors=[]` 가 "전체 정상" 신호로 오인 — LLM 레이어 무결성 별도 모니터링 필요
- **Brier score 재계산 불가**: confidence=0.3 flat + home_win_prob=null → 확률 기반 Brier 계산 오염 (n=117 대다수)
- **v1.8 cohort n 소폭 감소**: 118→117 (1건 재분류 추정) — 미세하나 원인 불명

## Next Candidates

1. **fix-incident: judge-agent 토론 복구** — Vercel prod LLM 실패 원인 조사. `judgeResult.success=false` 원인 확인 (API 키 환경변수 / 모델 ID / 응답 파싱 문제). `captureAgentFallbackAlert()` 추가 필요.

2. **review-code: silent drift wave 164** — 최근 wave 163 ISR sweep 이후 남은 리터럴 패턴 그레핑. `CURRENT_SCORING_RULE` / `HOME_ADVANTAGE_PCT` 등 추가 후보 grep.

3. **operational-analysis heavy (n≥150 도달 시)**: v2.0 가중치 확정. 현재 n=117, 잔여 33건, ETA 약 2026-07-15 (velocity ~2건/일 추정). 도달 시 즉시 heavy fire.

## Milestone: cycle 1400

- PASS_ship 누적 ≈ 892 (wave 153-163 = 11 waves + sparse detection)
- silent drift family streak ≈ 942 cycle (cycle 458 → 1400)
- v1.8 cohort: n=117 / 59.0%
- judge-agent 토론 비활성 22일 (미해결)
- 이번 주 최고 성과: 75.0% (정량 모델 단독)
