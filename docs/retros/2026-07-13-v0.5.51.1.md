# 회고 — v0.5.51.1 / 2026-07-13 / cycle 1545

## What (무엇을 했나)

`operational-analysis (lite)` — 25사이클 gap trigger(cycle 1520 마지막 발화) + cycle 1544 retro 권장.

- **W28 weekly-review 최종 집계** (07-07~07-09, 올스타 브레이크 전 3일)
  - 13경기 v1.8: 7/13 = 53.8%
  - CREDIT_EXHAUSTED 구간(07-07): 2/5 = 40%
  - 비CE 구간(07-08~09): 5/8 = 62.5%
  - 홈팀 실제 승률: 9/13 = 69.2%
- **v1.8 누적 실측** (DB 직접 쿼리): n=165, 58.8% 전체 / CE 56.3% / 비CE 66.7%
- **extract-pattern 3건** gstack learnings.jsonl 등록
- **plan #20 expired archive** (만료일 2026-06-07)
- **CHANGELOG** W28 섹션 최종 업데이트

## Decisions (결정과 이유)

| 결정 | 이유 |
|---|---|
| operational-analysis (lite) 선택 | 25-cycle gap 정확히 도달 + cycle 1544 retro 권장 |
| explore-idea 보류 | saturation=12/15 경계치지만 op-analysis가 더 직접적 trigger |
| 가중치 조정 없음 | v1.8 유지 확정(2026-07-06) + CREDIT_EXHAUSTED 환경에서 팩터 분석 신뢰도 낮음 |
| n=165 실측값 사용 | CLAUDE.md 기재 n=178(cycle 1460)과 -13 차이 → DB 실측 우선 |

## Worked (잘 됐던 것)

- **CE vs 비CE 분리 분석**: 10.4pp 성능 격차를 처음 수치로 확인. CREDIT_EXHAUSTED가 전체 정확도를 얼마나 끌어내리는지 명확.
- **올스타 브레이크 자동 탐지**: DB 쿼리 결과(07-10 이후 games = 0)로 브레이크 기간 자동 확인.
- **패턴 3건 체계화**: P1(CE 희석), P2(Elo fallback 홈보정 미작동), P3(scoring_rule 필터) — 모두 재사용 가능 수준으로 구조화.

## Didn't Work (아쉬운 것)

- **n 카운트 불일치**: CLAUDE.md n=178 vs DB 실측 n=165 (-13). 측정 기준 불일치 미해소.
  - 가설: v1.8 이전 predictions가 일부 포함됐거나, shadow 예측 카운트 차이
- **주간 리포트 블로그 발행**: insights DB 테이블 없음 → 블로그 형식 발행 경로 불명확. CHANGELOG 기록으로 대체.
- **팩터 개별 적중률 갱신 불가**: 팩터별 필드(home_sp_fip 등) 있지만 W28 CE 구간은 모두 중립값 → 팩터 신호 없음.

## Next Candidates (다음 3가지)

1. **`fix-incident (lite)` — n=165 vs n=178 불일치 원인 규명**
   - DB 쿼리: v1.8 predictions 전수 vs model_version 필터 각 쿼리 비교
   - shadow/postview 버전이 n에 포함됐는지 확인
   - CLAUDE.md 업데이트 (n 기준 명확화)

2. **`explore-idea (heavy)` — W29 피처 (올스타 후 재개)**
   - improvement saturation=12/15 해소 목적
   - 올스타 브레이크 후 첫 경기(07-14 월)부터 W29 예측 파이프라인 점검
   - 후보: 팀별 올스타 참가자 데이터 반영 / 브레이크 후 폼 리셋 로직

3. **`review-code (heavy)` — wave-245 stale cycle-ref sweep**
   - wave-244(cycle 1544)에서 kbo-data 정리 완료
   - wave-245 대상: CHANGELOG 위 `cycle NNN` 패턴 + TODOS.md 구버전 항목 sweep
   - silent drift family 연속성 유지
</content>
</invoke>