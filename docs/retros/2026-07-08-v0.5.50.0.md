# Retro: v0.5.50.0 — cycle 1517 operational-analysis lite (2026-07-08)

## What

- **operational-analysis (lite)** — 직전 2 cycle retro(1515/1516) 모두 op-analysis 권고 + 18-cycle gap trigger 접근
  - `/weekly-review` → `/extract-pattern` → `/compound` 시퀀스 실행
- **W27 최종 실측**: 26 resolved / 57.7% (CHANGELOG 기재 22/54.5% → 4경기 추가 verify)
- **W28 YTD** (7/6~7/7): 5 resolved / 40% — 초기, CREDIT_EXHAUSTED 32일+ 지속
- **팩터 실측 적중률** (n=300 resolved with factors):
  - sp_fip / sp_xfip: **65.7%** (최강)
  - bullpen_fip: 60.4%, lineup_woba: 58.8%
  - sfr: **50.0%** (랜덤 수준)
  - WAR / park_factor: **0.5 고착 = 데이터 결손 확인**
- **패턴 3개 추출** (CHANGELOG + learnings.jsonl):
  - P5: DB_CONSTRAINTS 단일 소스 + ESLint 차단 (quality_guard)
  - P6: 모델 팩터 실측 적중률 집계 (ai_agent)
  - Anti-P2: 데이터 수집 결손 → normalize 중립값 → 가중치 사문화 (anti_pattern)
- **전 사이클 커버리지** (cycle 1511~1516):
  - onConflict drift 방어 Scope A→D 완결 (wave 221~227)
  - /accuracy 팩터별 적중률 섹션 신규 (cycle 1516)

## Decisions

| 결정 | 이유 |
|---|---|
| 가중치 변경 없음 | v1.8 cycle 1460 확정. WAR/park_factor 버그가 원인 → 데이터 수정 후 재측정이 올바른 순서 |
| WAR/park_factor fix 이번 사이클 제외 | op-analysis lite scope = 측정·기록. fix-incident는 다음 cycle |
| v0.5.50.0 minor bump | cycle 1516 /accuracy 팩터 섹션 = 신규 사용자 가시 기능 |
| sfr 50.0% → 가중치 재검토 carry-over | n=300 통계적 유의. sfr=5% 가중치 축소 후보 (WAR fix 후 재측정 전제) |

## Worked

- **op-analysis lite trigger 자연 발화**: 직전 2 cycle 모두 권고 + 18-cycle gap → 즉각 선택
- **데이터 결손 탐지**: WAR/park_factor 0.5 고착 패턴을 factor accuracy 분석으로 최초 발견
  - `total_war=0.0` 전 팀 + stadium 키 미스매치 두 독립 버그 동시 확인
  - 기존 sparse_neutral_count alert 외 별도 데이터 수집 검증 레이어 필요성 확인
- **W27 갱신**: CHANGELOG 기재(22/54.5%) → 최종(26/57.7%) 4경기 추가 verify 반영
- **팩터 기준선 확립**: n=300 실측 → sp_fip 65.7% / sfr 50.0% 수치 박제

## Didn't Work

- **CREDIT_EXHAUSTED 32일+ 지속**: 사용자 Anthropic 크레딧 미충전 (2026-06-06~)
  - 모든 예측 conf=0.3 flat → 고확신 예측 없음
  - Elo 팩터 n=1 (실질적 0) → KBO 팀 간 Elo 차이 미미 (1440~1550 범위)
- **WAR/park_factor 12% 사문화**: cycle 1511~1516 6 cycle 동안 탐지 못함
  - factor_accuracy UI가 사후 탐지기 역할 수행 → proactive 감지 부재
- **W28 YTD 40%**: 소표본 (5 games), 추세 판단 불가

## Next Candidates

1. **fix-incident (heavy)**: WAR + park_factor 데이터 결손 2건 동시 수정
   - WAR: `team_season_stats.total_war = 0.0` → KBO Fancy Stats WAR 스크래퍼 점검 + DB 갱신
   - park_factor: `games.stadium` 단축명 → `DEFAULT_PARK_FACTORS` 전체명 매핑 테이블 추가
   - 수정 후: `buildFactorAccuracy` 재실행 → Brier delta 측정 → sfr 5% 가중치 재검토 판단
   - 우선순위: **HIGH** — 12% 가중치 사문화, 즉시 수정 가능한 버그

2. **fix-incident (medium)**: CREDIT_EXHAUSTED 충전 후 debate 복구 검증
   - `debate.ts` conf 히스토그램 복귀 확인 (0.3 flat → 다양한 값)
   - 충전 후 첫 7일 acc 실측 → baseline(60.9%) 재비교
   - 사용자 Anthropic 크레딧 충전 필요 (자율 조치 불가)

3. **explore-idea (heavy)**: sfr 가중치 조정 검토
   - sfr 50.0% (n=176) → 랜덤 수준 → 5% 가중치 축소 후보
   - 전제: WAR/park_factor fix 후 팩터 기준선 재측정
   - 방향: sfr 5%→2% + 해방된 3%를 sp_fip 또는 bullpen_fip 으로 재배분
   - 우선순위: MEDIUM (WAR fix 후 수치 변동 후 결정)
