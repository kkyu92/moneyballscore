# Retro — v0.5.28 (2026-05-07) · cycle 207 · operational-analysis (lite)

## What

이번 사이클은 121 사이클 만의 첫 operational-analysis (lite) 진입.
코드 변경 없이 운영 데이터 분석 + 패턴 추출 + CHANGELOG 기록.

### 수행 작업
- Supabase REST API 직접 쿼리로 4월 이후 72건 verified 예측 전수 분석
- 주별 적중률 추이 (W17: 54.1% → W18: 36.0% 급락 → W19: 60.0% 회복)
- Brier Score, Calibration, 팩터별 방향성 차이 측정
- 반복 실패 매치업 3쌍 식별 (SSGvs롯데 0/3, 삼성vsSSG 0/3, 키움vs삼성 0/3)
- SFR 팩터 음수 이상값 버그 발견 (normalize 함수 음수 입력 미처리)
- gstack learnings에 4개 패턴 등록
- CHANGELOG.md [0.5.28] 섹션 작성

## Decisions

**가중치 조정 보류**: 72건 < 100건 임계. CI95 범위 40pp+ 이상이라 측정값 신뢰 불충분.
  → 100건 도달 예상: 5월 2주차 (~5/14)

**SFR 버그 즉시 수정 X**: Brier 영향 ≈ 0.0002 (미미). 다음 fix-incident 사이클에 carry-over.

**operational-analysis lite 성립**: 6건/72건 SFR 음수 = 데이터 신선도 문제 발견 → success 박제.

## Worked

- Supabase REST API를 CLI 없이 직접 쿼리해서 72건 전수 집계 성공
- W18 급락(36%) 원인을 SSG vs 롯데 3연전 구조적 실패로 정확히 pinpoint
- calibration bucket 분석으로 역전 구간(conf=0.4 > conf=0.5) 발견
- gstack learnings에 4개 패턴 구조화 완료 (problem→solution→results 형식)
- extract-pattern + compound 스킬 시퀀스 자연 진행

## Didn't Work

- 정확한 주별 구분 로직: ISO week 계산 대신 날짜 조건문으로 대체 (2026-W18 경계 애매)
- SFR 팩터 분포가 초기부터 음수 포함임을 인지 못해 이상값 원인 파악에 추가 조회 필요
- head_to_head 팩터 0.0 값 18건/72건 (데이터 없음) → 이 경우 0.5 neutral로 처리되는지 확인 필요

## Next Candidates

1. **fix-incident: SFR normalize 음수 처리 버그**
   - `predictor.ts:normalize()` 입력값 음수 시 factor 0~1 범위 이탈
   - 수정: `Math.max(0, val)` 클램핑 또는 `(a-b)/(|a|+|b|+ε)` → [0,1] 재매핑
   - 영향: Brier 미미하나 개념적 정합성 + 향후 가중치 조정 시 오염 방지

2. **operational-analysis (heavy): 100건 임계 도달 시 가중치 v2.0 측정**
   - 예상 도달: 5월 2주차 (~5/14). head_to_head 5%→8%, elo 8%→5% 후보
   - head_to_head 팩터 0.0 케이스(데이터 없음) 처리 개선도 함께 검토

3. **review-code (heavy): predictor.ts 팩터 계산 전반 review**
   - SFR 음수 버그 계기로 10팩터 normalize 로직 전수 검토
   - recent_form avg=0.458 (avg<0.5 = 홈팀 폼 약세 전반?) 원인 파악
   - head_to_head 0.0 케이스가 실제로 어떤 경기에서 발생하는지 확인

## Metrics

| 지표 | 값 |
|---|---|
| 이번 주 적중률 (W19) | 6/10 = 60.0% |
| 직전 주 적중률 (W18) | 9/25 = 36.0% |
| 4월 이후 누적 | 35/72 = 48.6% |
| Brier Score (누적) | 0.2494 |
| SFR 이상값 | 6/72건 |
| 0% 매치업 | 3쌍 |
| gstack learnings 추가 | 4건 |
