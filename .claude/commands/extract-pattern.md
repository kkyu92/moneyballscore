최근 작업에서 재사용 가능한 패턴을 추출하세요.

카테고리: $ARGUMENTS (data_pipeline / ai_agent / quality_guard / content_auto / anti_pattern)

1. 최근 커밋과 변경사항에서 패턴 후보 식별
2. 문제(problem) → 해결(solution) → 결과(results) 구조로 정리
3. 해당 패턴이 다른 프로젝트에서도 재사용 가능한지 판단
4. CHANGELOG.md에 기록 + gstack learnings에 등록
